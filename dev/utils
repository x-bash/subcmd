
function debug(msg){
	print msg > "/dev/stderr"
}

function char_at(s, idx) {
    return substr(s, idx, 1)
}

function char_is_number(c,      o) {
    o = _ord_[c]
    if ( (o >= 48) && (o <= 57) ) {
        return true
    }
    return false
}

